# WebMumble Caddy Configuration
# Proxy mode - forwards to running dev server

mumble.horse.xxx {
    # Proxy everything to Vite dev server
    # Vite already handles WebSocket proxy to backend:9847
    reverse_proxy localhost:3000
}
